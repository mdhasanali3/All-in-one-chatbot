# secrets.yaml - Base template for secrets
# In production, use sealed secrets or external secret management
apiVersion: v1
kind: Secret
metadata:
  name: ai-assistant-secrets
type: Opaque
stringData:
  # Replace with actual values
  keycloak-client-secret: "your-keycloak-client-secret"
  access-key: "admin_hasan_007_no_exit"
  openai-api-key: "your-openai-api-key"
  elevenlabs-api-key: "your-elevenlabs-api-key"
  jwt-secret-key: "your-jwt-secret-key"
